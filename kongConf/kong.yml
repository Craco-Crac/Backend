_format_version: "3.0"
_transform: false

services:
  - name: python
    host: python-service
    port: 3000

  - name: docs
    host: docs
    port: 3000

  - name: node_s
    host: node-service
    port: 3000

  - name: user_management
    host: user-manage
    port: 3000

routes:
  - service: user_management
    paths:
      - /users
    strip_path: true

  - service: docs
    paths:
      - /docs
    strip_path: true

  - service: node_s
    paths:
      - /node
    strip_path: true

  - service: python
    paths:
      - /python
    strip_path: true